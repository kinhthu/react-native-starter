// saga effects

// import { put, takeEvery } from 'redux-saga/effects';

// import { INCREASE, DECREASE } from '../actions/actionType';
// import { increase, decrease } from '../actions';

// function* increaseSaga(amount) {
//   yield put(increase(amount));
// }

// export function* watchIncrease(amount) {
//   yield takeEvery(INCREASE, increaseSaga(amount));
// }

// function* decreaseSaga(amount) {
//   yield put(decrease(amount));
// }

// export function* watchDecrease(amount) {
//   yield takeEvery(DECREASE, decreaseSaga(amount));
// }
