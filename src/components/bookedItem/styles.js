import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    // width,
  },
  nameStyle: {
    color: 'green',
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 10,
  },
  priceStyle: {
    color: 'gray',
  },
  buttons: {
    flexDirection: 'row',
    height: '100%',
  },
  button: {
    height: '100%',
    padding: 15,
    borderRadius: 0,
  },
});

export default styles;
